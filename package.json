{
  "name": "mates",
  "version": "0.2.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build --target aarch64 --apk",
    "android:build:signed": "bash -lc 'source ./android-signing.env && tauri android build --target aarch64 --apk'",
    "android:build:all": "tauri android build",
    "proxy:dev": "pnpm --filter baserow-proxy dev",
    "proxy:deploy": "pnpm --filter baserow-proxy wrangler:deploy",
    "proxy:secrets": "pnpm --filter baserow-proxy secrets",
    "proxy:secret:put": "pnpm --filter baserow-proxy secret:put",
    "proxy:login": "pnpm --filter baserow-proxy login",
    "version:bump:patch": "node scripts/bump-version.mjs patch",
    "version:bump:minor": "node scripts/bump-version.mjs minor"
  },
  "packageManager": "pnpm@9.15.4",
  "license": "GPL-3.0",
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "@tauri-apps/cli": "^2"
  }
}
